class ServerRoot{
  // TODO: move here list of games and list of connections
  // game can have link to its connections
  // server root or some dedicated service should take care of all connection handling

  // idea: what if there isn't routing on client-side, just on server-side and every reload lead to place,
  // where logged user is?
}