part of user_utils;

Future<shelf.Response> getStartingUnits(String userEmail, String heroId, ManagedContext context) {
//  var heroesQuery = Query<Hero>(context)..where((hero) => hero.user.id).equalTo(userId);
//  return heroesQuery.fetchOne();
}
