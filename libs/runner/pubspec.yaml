name: runner
description: runner
version: 0.0.1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  shared:
    path: '../../shared'
  utils:
    path: '../utils'
  io_utils:
    path: '../io_utils'
