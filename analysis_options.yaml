analyzer:
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false
  exclude:
    - from_prev_versions/**
    - class_generator/**

linter:
  rules:
    - avoid_empty_else
    - cancel_subscriptions
    - close_sinks